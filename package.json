{
  "name": "collabboard-root",
  "version": "1.0.0",
  "description": "Run CollabBoard backend and frontend together",
  "scripts": {
    "start": "concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev --prefix server\" \"npm start --prefix syncboard-client\"",
    "install-all": "npm install --prefix server && npm install --prefix syncboard-client",
    "build-client": "npm run build --prefix syncboard-client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}